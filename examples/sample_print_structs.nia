struct Pair(i32, i32)
struct Point {
    x: i32,
    y: i32,
    p: &i32,
}

fn main() i32 {
    let p: Pair = Pair(1, 2);
    println(p);

    let a: i32 = 7;
    let pa: &i32 = &a;
    let pt: Point = Point { x: 10, y: 20, p: pa };
    println(pt);
    println(pa);
    0
}
