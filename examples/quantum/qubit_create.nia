quant fn prepare(q: qubit) {
    H(q);
    let r = q_measure(q);
    q_record(r);
}

fn main() i32 {
    quant {
        let a = qubit();
        let b: qubit = qubit();
        prepare(a);
        prepare(b);
    }

    0
}
