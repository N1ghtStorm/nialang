struct Point { x: i32, y: i32 }

impl Point {
    fn sum(&self) i32 {
        self.x + self.y
    }

    fn add(self, n: i32) i32 {
        self.sum() + n
    }
}

fn main() i32 {
    let p = Point { x: 2, y: 3 };
    p.add(4)
}
