fn main() i32 {
    let shared: Arc[i32] = arc_new(7);
    let worker_shared = shared.clone();

    let t: Thread = spawn move || {
        println(*worker_shared);
    };
    join(t);

    println(*shared);
    drop(shared);
    0
}
