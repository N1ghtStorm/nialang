enum Color { Red, Green, Blue }

fn score(c: Color) i32 {
    match c {
        Color::Red => 1,
        Color::Green => 2,
        Color::Blue => 3,
    }
}

fn main() i32 {
    let c = Color::Green;
    println(score(c));
    0
}
