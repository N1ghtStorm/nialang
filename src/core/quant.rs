//! Quantum primitives in Core (phase 13).

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum QuantKind {
    QubitNew,
    Measure,
    Read,
    Record,
    GateI,
    GateH,
    GateX,
    GateY,
    GateZ,
    GateS,
    GateSdg,
    GateT,
    GateTdg,
    GateCnot,
    GateCz,
    GateSwap,
    GateCh,
    GateCy,
    GateCs,
    GateCsdg,
    GateCt,
    GateCtdg,
    GateCcnot,
    GateCcz,
    GateCswap,
    GateRx,
    GateRy,
    GateRz,
    GateR1,
    GateCrx,
    GateCry,
    GateCrz,
    GateCr1,
}
