[package]
name = "nialang"
version = "0.1.0"
edition = "2024"

[features]
qir-runner = ["dep:runner"]

[dependencies]
runner = { path = "../QIR_REPOS_FOR_AI_CONTEXT/qir-runner/runner", optional = true }
