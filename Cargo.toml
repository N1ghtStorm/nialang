[package]
name = "nialang"
version = "0.1.0"
edition = "2024"

[features]
qir-runner = ["dep:runner"]

[dependencies]
runner = { git = "https://github.com/N1ghtStorm/qir-runner", branch = "main", optional = true }
